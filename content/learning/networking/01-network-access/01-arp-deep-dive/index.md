---
title: "ARP Deep Dive: วิเคราะห์การระบุตัวตนด้วย MAC Address"
date: 2025-12-21
draft: true
categories: ["Networking"]
series: ["Cyber Investigator Roadmap"]
tags: ["draft", "investigation"]
cover:
    image: "cover.jpg"
    relative: true
---

## 🔍 บทนำ
(เนื้อหาเริ่มต้นของบทความ ARP Deep Dive: วิเคราะห์การระบุตัวตนด้วย MAC Address)

 บทนี้สำคัญมาก เพราะถ้าเราไม่เข้าใจว่าอุปกรณ์ระบุตัวตนกันอย่างไรในระดับล่างสุด เราจะไม่มีทางรู้เลยว่า "ใครคือใคร" ในวงแลนของเรา

---

## 🌐 01-ARP Deep Dive: วิเคราะห์การระบุตัวตนด้วย MAC Address

ในโลกของ IP Address เราอาจจะรู้จักกันในชื่อ "บ้านเลขที่" แต่ในความเป็นจริงของระบบเครือข่ายคอมพิวเตอร์ไม่ได้ส่งข้อมูลผ่าน IP โดยตรง แต่มันส่งผ่าน **MAC Address** (เปรียบเสมือนรหัสบัตรประชาชนของอุปกรณ์)

**ARP (Address Resolution Protocol)** คือโปรโตคอลที่ทำหน้าที่เป็น "ล่าม" แปลงจาก IP ให้กลายเป็น MAC Address เพื่อให้ข้อมูลส่งไปถึงเครื่องที่ถูกต้องจริงๆ

### 🔍 กลไกการทำงานของ ARP (The Flow)

เมื่อเครื่อง A (192.168.1.5) ต้องการคุยกับเครื่อง B (192.168.1.10) สิ่งที่เกิดขึ้นคือ:

1. **ARP Request (Broadcast):** เครื่อง A จะตะโกนบอกทุกคนในวงแลนว่า _"ใครคือ 192.168.1.10? ช่วยบอก MAC Address ให้ฉัน (192.168.1.5) หน่อย!"_
    
2. **ARP Reply (Unicast):** เครื่อง B ที่มี IP นั้นอยู่ จะตอบกลับเครื่อง A โดยตรงว่า _"ฉันเอง 192.168.1.10 และนี่คือ MAC Address ของฉัน: `AA:BB:CC:DD:EE:FF`"_
    

> **Note สำหรับนักสืบ:** ข้อมูลนี้จะถูกเก็บไว้ใน **ARP Cache** ชั่วคราวเพื่อให้ไม่ต้องตะโกนถามบ่อยๆ เราสามารถดูได้ด้วยคำสั่ง `ip neigh` หรือ `arp -a` ใน Linux ครับ

---

### 🛠 เครื่องมือและคำสั่งสำหรับการวิเคราะห์

ในการสืบสวน เราจะใช้ **Tshark** เพื่อ "ดักฟัง" การตะโกนถามเหล่านี้ครับ

#### 1. การจับ Packet ARP ทั้งหมดในวงแลน

Bash

```
sudo tshark -f "arp" -i eth0
```

- `-f "arp"`: คือการใส่ Capture Filter ให้เอาเฉพาะ ARP เท่านั้น
    
- คุณจะเห็นบรรทัดที่เขียนว่า `Who has 192.168.1.1? Tell 192.168.1.5`
    

#### 2. การดูรายละเอียดระดับลึก (Deep Analysis)

หากต้องการเห็นข้อมูลที่ละเอียดขึ้นเพื่อหาพิรุธ:

Bash

```
sudo tshark -f "arp" -V -c 2
```

สังเกตที่ส่วนของ **Address Resolution Protocol (request/reply)**:

- **Sender MAC/IP:** ใครเป็นคนถาม
    
- **Target MAC/IP:** ถามหาใคร
    

---

### 🕵️ จุดพิรุธที่นักสืบต้องมองหา (Investigative Points)

ในการสืบสวนเครือข่าย ARP มักถูกใช้เป็นจุดโจมตีที่เรียกว่า **ARP Spoofing** หรือการสวมรอย สิ่งที่เราต้องระวังคือ:

1. **Gratuitous ARP:** การที่อยู่ๆ มีเครื่องหนึ่งประกาศตัวว่า _"ฉันคือ IP นี้ (โดยที่ไม่มีใครถาม)"_ บ่อยผิดปกติ อาจเป็นการพยายามดักฟังข้อมูล (MITM)
    
2. **MAC Address ซ้ำ:** หากในวงแลนมี IP เดียวกันแต่มี MAC Address ต่างกันโผล่มาสลับไปมา นั่นคือสัญญาณของการโจมตี
    
3. **เครื่องแปลกปลอม:** การใช้เครื่องมืออย่าง `arp-scan` จะช่วยให้เราเห็น "ผู้บุกรุก" ที่แอบเชื่อมต่อ Wi-Fi เราอยู่
    
    Bash
    
    ```
    sudo arp-scan --localnet
    ```
    

---

### 📝 สรุปภารกิจในบทนี้

การเข้าใจ ARP คือการเข้าใจ **"ความเชื่อใจ"** ในวงแลน เพราะเครื่องคอมพิวเตอร์มักจะเชื่อคำตอบที่ได้รับมาทันทีโดยไม่ตรวจสอบ (Stateless) นี่จึงเป็นช่องโหว่พื้นฐานที่สุดที่นักสืบต้องเข้าใจก่อนจะข้ามไปวิเคราะห์ Layer ที่สูงขึ้นครับ

---

### 💡 ขั้นตอนต่อไปที่คุณควรลอง:

1. ลองรัน `tshark -f "arp"` ทิ้งไว้ แล้วลองใช้มือถือต่อ Wi-Fi เข้ามาใหม่ คุณจะเห็น Packet การประกาศตัวตนทันที
    
2. ลองรัน `arp-scan` เพื่อดูว่าในบ้านคุณมีอุปกรณ์กี่ชิ้น และ MAC Address ของแต่ละชิ้นตรงกับยี่ห้ออุปกรณ์จริงไหม?
    

